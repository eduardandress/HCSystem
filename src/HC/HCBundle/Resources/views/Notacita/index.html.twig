{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Notacita list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Idnotacita</th>
                <th>Presionarterial</th>
                <th>Alturapaciente</th>
                <th>Pesopaciente</th>
                <th>Frecuenciacardiaca</th>
                <th>Temperatura</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('notacita_show', { 'id': entity.idnotacita }) }}">{{ entity.idnotacita }}</a></td>
                <td>{{ entity.presionarterial }}</td>
                <td>{{ entity.alturapaciente }}</td>
                <td>{{ entity.pesopaciente }}</td>
                <td>{{ entity.frecuenciacardiaca }}</td>
                <td>{{ entity.temperatura }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('notacita_show', { 'id': entity.idnotacita }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('notacita_edit', { 'id': entity.idnotacita }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('notacita_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
