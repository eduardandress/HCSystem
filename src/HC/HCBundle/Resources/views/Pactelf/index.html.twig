{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Pactelf list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Idpactelf</th>
                <th>Numero</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('pactelf_show', { 'id': entity.idpactelf }) }}">{{ entity.idpactelf }}</a></td>
                <td>{{ entity.numero }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('pactelf_show', { 'id': entity.idpactelf }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('pactelf_edit', { 'id': entity.idpactelf }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('pactelf_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
