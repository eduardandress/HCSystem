{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Phciconsumo</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Idphciconsumo</th>
                <td>{{ entity.idphciconsumo }}</td>
            </tr>
            <tr>
                <th>Fechaactualizacion</th>
                <td>{{ entity.fechaactualizacion|date('Y-m-d H:i:s') }}</td>
            </tr>
            <tr>
                <th>Estado</th>
                <td>{{ entity.estado }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('phciconsumo') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('phciconsumo_edit', { 'id': entity.idphciconsumo }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
