{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Referencia list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Idreferencia</th>
                <th>Nombredoctor</th>
                <th>Descripcion</th>
                <th>Fecha</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('referencia_show', { 'id': entity.idreferencia }) }}">{{ entity.idreferencia }}</a></td>
                <td>{{ entity.nombredoctor }}</td>
                <td>{{ entity.descripcion }}</td>
                <td>{% if entity.fecha %}{{ entity.fecha|date('Y-m-d H:i:s') }}{% endif %}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('referencia_show', { 'id': entity.idreferencia }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('referencia_edit', { 'id': entity.idreferencia }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('referencia_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
