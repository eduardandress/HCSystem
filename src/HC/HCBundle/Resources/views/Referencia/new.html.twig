{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Referencia creation</h1>

    {{ form(form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('referencia') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}
